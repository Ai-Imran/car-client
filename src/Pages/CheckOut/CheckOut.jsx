import React from 'react';
import PropTypes from 'prop-types';

const Checkout = props => {
    return (
        <div>
            <h2>Check Out</h2>
        </div>
    );
};

Checkout.propTypes = {
    
};

export default Checkout;